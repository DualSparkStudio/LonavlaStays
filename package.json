{
  "name": "lonavala-stays",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix resort-booking",
    "build": "npm run build --prefix resort-booking",
    "lint": "npm run lint --prefix resort-booking",
    "preview": "npm run preview --prefix resort-booking"
  }
}
